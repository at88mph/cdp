apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cred.fullname" . }}-test-connection"
  labels:
    {{- include "cred.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cred.fullname" . }}:{{ .Values.service.port }}{{ .Values.application.availabilityPath }}']
  restartPolicy: Never
