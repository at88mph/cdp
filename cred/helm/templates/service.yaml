apiVersion: v1
kind: Service
metadata:
  name: {{ include "cred.fullname" . }}
  labels:
    {{- include "cred.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cred.selectorLabels" . | nindent 4 }}
